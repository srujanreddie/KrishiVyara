const q = "wheat field";
// We can just use known good unsplash IDs or search via source.unsplash.com equivalent (which is deprecated).
// Since it's deprecated, maybe I can just fetch a few images using Unsplash API? I don't have a key.
// Let's use Wikipedia images where possible.
